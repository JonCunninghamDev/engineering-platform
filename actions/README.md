# Reusable Actions

Composite or JavaScript actions shared by multiple workflows belong here.

Actions must document inputs, outputs, permissions, failure behavior, versioning, and rollback. Do not hide product-specific deployment or credential assumptions inside a general action.
