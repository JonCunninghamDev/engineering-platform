# Repository Profiles

Profiles compose shared policy for common repository shapes without embedding product-specific behavior.

Planned initial profiles:

- `node-python`
- `node-python-blender`

Profiles declare capabilities and defaults. Consumer repositories retain control of their commands, architecture, risk exceptions, and environments.
