# Schemas

Versioned machine-readable contracts belong here.

The first planned schema is `engineering-policy/v1`, which will define branch roles, profiles, capabilities, checks, merge authority, promotion gates, and explicit local overrides.

Schema changes require fixtures, compatibility documentation, and human review.
